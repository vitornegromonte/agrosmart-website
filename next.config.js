/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'www.ycombinator.com', 'upload.wikimedia.org'],
  },
}

module.exports = nextConfig 